<h1>Welcome <%= @patient.name %></h1>
<ul>
<% @patient.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
    with <%=link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
<% end %>
</ul>